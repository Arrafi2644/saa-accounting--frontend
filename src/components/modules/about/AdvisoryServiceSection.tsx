import AdvisoryServiceClient from "./AdvisoryServiceClient";

export default function AdvisoryServicesSection() {
    return (
        <section className="w-full bg-[#cff0fc78] py-20 xl:py-28 px-4 sm:px-6 lg:px-8">
            <AdvisoryServiceClient />
        </section>
    );
}