import AccountingHeroClient from './AccountingHeroClient';

export default function AccountingHeroSection() {
    return (
        <section className="w-full py-20 px-4 sm:px-6 lg:px-8">
            <div className="container mx-auto">
                <AccountingHeroClient />
            </div>
        </section>
    );
}